import 'package:flutter/material.dart';

class ScrabbleKeyboard extends StatelessWidget {
  const ScrabbleKeyboard({super.key});

  @override
  Widget build(context) {
    return Container(child: const Text("Keyboard"));
  }
}
